#include <bits/stdc++.h>

using namespace std;

/**
 * queue：為FIFO的資料結構
 * 1. Member functions
 *  1.1 queue() // ()內可為container或陣列
 *  1.2 empty()
 *  1.3 size()
 *  1.4 front() // access the first element of queue
 *  1.5 back()  // access the last element of queue
 *  1.6 push()  // insert element
 *  1.6 emplace() // insert element, but new element is constructed in place using args as the arguments for its constructor
 *  1.7 pop()  // remove element
 *  1.8 swap(queue& x) // Swap the all elements of two queues
*/

int main()
{
    system("pause");
    return 0;
}